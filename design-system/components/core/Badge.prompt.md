Compact status or category pill, monospace. Neutral by default; tint it with `color` / `bg`.

```jsx
<Badge>в работе</Badge>
<Badge color="var(--warning)">ожидание</Badge>
<Badge color="var(--accent)" bg="var(--accent-dim)">фокус</Badge>
```
