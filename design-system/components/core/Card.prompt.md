Flat terminal surface. Pass `title` for a terminal title bar (● TITLE ✕); `well` for the recessed variant; `interactive` for a hover glow on clickable cards.

```jsx
<Card title="новая задача" onClose={close}>
  <Input label="название" />
</Card>
<Card well>// пусто</Card>
```
