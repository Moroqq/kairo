Centered modal dialog. Terminal title bar, neon-bordered panel, blurred scrim. Closes on Escape, scrim click, and ✕.

```jsx
<Modal open={open} onClose={close} title="режим оформления" width={420}>
  <div style={{ padding: 12 }}>…</div>
</Modal>
```
